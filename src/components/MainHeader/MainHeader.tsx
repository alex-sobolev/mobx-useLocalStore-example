import React, { FC, useEffect } from 'react';
import './MainHeader.css';

export const MainHeader: FC = () => {
  return (
    <header className="MainHeader">Mobx with functional components and `useLocalStore` hook</header>
  );
};
